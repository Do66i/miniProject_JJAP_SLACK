import Workspace from '@layouts/Workspace';
import React from 'react';

const DirectMessage = () => {
  return (
    <Workspace>
      <div>DM!</div>
    </Workspace>
  );
};

export default DirectMessage;
